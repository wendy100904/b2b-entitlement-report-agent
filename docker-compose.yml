services:
  entitlement-agent:
    build: .
    ports:
      - "8000:8000"
    environment:
      OPENAI_API_KEY: ${OPENAI_API_KEY:-}
      OPENAI_MODEL: ${OPENAI_MODEL:-gpt-5}
      MAX_UPLOAD_MB: ${MAX_UPLOAD_MB:-30}
